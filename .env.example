# Copiar como .env.local (no se versiona) y completar.

# URL del backend vista desde el teléfono: la IP de tu computadora en la red, no "localhost".
EXPO_PUBLIC_API_URL=http://192.168.0.10:8080

# Firebase Realtime Database (configuración de la app web del proyecto de Firebase).
EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_DATABASE_URL=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=

# Segundos sin recibir la posición de una unidad para mostrar "última actualización hace X" (PB-06 R3).
EXPO_PUBLIC_UMBRAL_POSICION_SEG=60

# Centro del mapa para fijar el pin cuando el teléfono no conoce una posición reciente (PB-02 R2).
# Coordenadas de la ciudad donde opera el servicio, en grados decimales. Si faltan o no son válidas, se ve Bolivia entera.
EXPO_PUBLIC_MAPA_LATITUD=
EXPO_PUBLIC_MAPA_LONGITUD=

# Minutos que puede tener la última posición conocida del teléfono para arrancar ahí el mapa del pin.
# Si es más vieja, el pin espera a que la persona mueva el mapa hasta donde está.
EXPO_PUBLIC_EDAD_MAXIMA_UBICACION_MIN=5

# Clave de Google Maps para Android (solo development build). No lleva EXPO_PUBLIC_: la lee app.config.ts.
GOOGLE_MAPS_API_KEY=
